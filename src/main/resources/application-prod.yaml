server:
  port: 8866
spring:

  application:
    name: classManager
  datasource:

    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://bt.00sg.cn:3306/reboot
    username: reboot
    password: 8n6ZAFHWr4p5tWcM

  mail:
    username: 264494336@qq.com
    password: yqmmenyzdqnfcbcg
    host: smtp.qq.com
    properties:
      mail:
        smtl:
          ssl:
            enable: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id
  mapper-locations: classpath:/mapper/*.xml

user:
  visitedId: 2